/**
 * generates a random number between 0 to numMovies
 */
function getRandomNumber(numMovies) {
	
	 return Math.floor((Math.random() * numMovies)); 

}

/**
 * generate the information to be displayed on the modal
 */
function showDetails(imdbID) {
	
	console.log(imdbID);

}

function randomThrow() {

	

}